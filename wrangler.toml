# Cloudflare Pages configuration.
# Build command: npm run build   (set in the Pages dashboard or here)
# API lives in /functions (file-based routing → /api/*).
# Set the WEATHER_API_KEYS secret in: Pages → Settings → Environment variables.
name = "instant-weather"
pages_build_output_dir = "dist"
compatibility_date = "2024-11-01"
