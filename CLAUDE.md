# Instant Weather — project rules

## Git commits

- Author every commit as the sole developer. Subjects and bodies use the
  imperative, first-person singular — "Add OG tags", "Fix hue transition".
  Never "we", "let's", "I've gone ahead".
- **No AI or tool attribution anywhere** — no `Co-Authored-By: Claude`,
  `Generated with Claude Code`, `Claude-Session`, or any variant, in commit
  messages, trailers, or PR bodies. Also enforced by `.claude/settings.json`
  (attribution disabled) and the `.githooks/commit-msg` hook.
- Match the surrounding code's comment density and idiom. No decorative block
  headers, no explanatory comments on obvious code, no "generated by" notices.

## Author

Uday Kanth Tirukkovaluri <t.uday.kanth12@gmail.com>

## Licensing

Split: source code is MIT (`LICENSE`); the "Instant Weather" name, the aurora
theme and visual design, written copy, and image assets under `public/` are All
Rights Reserved (`NOTICE`).

## Hook setup

`npm install` runs `simple-git-hooks` (via `postinstall`), which installs both
the `pre-commit` (lint-staged) and `commit-msg` (`.githooks/commit-msg`) hooks.
If commits happen before an install, run `npx simple-git-hooks` once by hand.
